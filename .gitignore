# 빌드 결과물
Binaries/
Build/
Intermediate/
Saved/
DerivedDataCache/

# 대용량 외부 에셋 - 로컬에서만 사용, Git에는 포함하지 않음
Content/ParagonAurora/

# IDE
.vs/
.idea/
*.VC.db
*.VC.opendb
*.sln

# 파생 파일
FileOpenOrder/
*.suo
*.user

# 에셋은 커밋 (Content 폴더)
# 단, 용량 큰 에셋은 Git LFS 사용 권장